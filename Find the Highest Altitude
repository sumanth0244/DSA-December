class Solution {
    public int largestAltitude(int[] gain) {
        int a=0;
        int ma=0;
        for(int i:gain){
            a=a+i;
            ma=Math.max(a,ma);
        }
        return ma;
    }
}
